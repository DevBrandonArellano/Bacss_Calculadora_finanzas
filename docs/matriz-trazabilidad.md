# Matriz de trazabilidad — cierre (Fase 14)

> Completa la sección 10 del plan de desarrollo con rutas reales del proyecto terminado.

## 1. Estándares y prácticas → implementación → verificación

| Estándar / práctica                                                       | Dónde se materializa                                                                                                                                                                                                                                                                  | Cómo se verifica                                                                                                                                                                                                                               |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **SOLID**                                                                 | `AmortizationSystem` (Strategy: `FrenchAmortization`/`GermanAmortization`), `ExtraPaymentStrategy` (Strategy: `ReduceTermStrategy`/`ReducePaymentStrategy`), puertos `Logger`/`ScenarioRepository`/`Exporter`, casos de uso dependiendo de abstracciones (`src/application/`)         | `amortizationSystem.contract.test.ts` (LSP: cualquier sistema pasa el mismo contrato); revisión de imports reforzada por `eslint-plugin-boundaries`                                                                                            |
| **Clean Code**                                                            | Nomenclatura explícita, funciones cortas, sin números mágicos (`RoundingPolicy`, `RateConvention` vía `InterestRate`), errores tipados (`domain/shared/errors.ts`)                                                                                                                    | `npm run lint` (ESLint + `typescript-eslint`), revisión de PR                                                                                                                                                                                  |
| **ISO 25010**                                                             | Fiabilidad: errores tipados + invariantes verificadas en `runAmortization`. Mantenibilidad: cobertura mínima por capa (`vitest.config.ts` `thresholds`). Usabilidad: mensajes de error traducidos (`translateDomainError`), disclaimer siempre visible. Seguridad: ver fila ISO 27001 | `npm run test:coverage`; `DebtVsInvestmentPanel.test.tsx` (disclaimer); `loanStore.test.ts` (traducción de errores)                                                                                                                            |
| **ISO 27001**                                                             | CSP solo en build de producción ([ADR 0008](adr/0008-csp-solo-en-build-produccion.md)); sin `eval`/`innerHTML`; redacción de campos sensibles en `Rfc5424Logger`; `npm audit` en CI                                                                                                   | `e2e/accessibility.spec.ts` (axe no cubre CSP, pero valida que la app funciona bajo la política real vía `test:e2e` contra el build); job `audit` en `.github/workflows/ci.yml`; `rfc5424Logger.test.ts` (redacción)                           |
| **RFC 5424**                                                              | `Rfc5424Logger` (8 niveles de severidad, `src/infrastructure/logging/rfc5424Logger.ts`)                                                                                                                                                                                               | `rfc5424Logger.test.ts`; tests de cada caso de uso que verifican `severity: 'error'` al loguear un `DomainError` (`calculateSchedule.test.ts`, `simulateExtraPayments.test.ts`, `compareScenarios` y `compareDebtVsInvestment` vía sus stores) |
| **Manejo de errores**                                                     | Jerarquía `DomainError` → `InvalidInputError`/`NegativeAmountError`/`OutOfRangeError`/`CurrencyMismatchError` (`domain/shared/errors.ts`); nunca `catch` vacío                                                                                                                        | `errors.test.ts`; `translateDomainError` cubierto indirectamente por `loanStore.test.ts`, `scenarioStore.test.ts`, `investmentStore.test.ts`                                                                                                   |
| **TDD (Red-Green-Refactor)**                                              | Todo el dominio y la mayoría de la presentación                                                                                                                                                                                                                                       | Historial de commits de la sesión de desarrollo; cada componente/store tiene su `.test.ts(x)` par a la implementación                                                                                                                          |
| **Caja blanca**                                                           | Cobertura de sentencias/ramas por capa (`vitest.config.ts`: 95 % `domain/shared`; 90 % `domain/loans`, `domain/investments`, `application/use-cases`, `infrastructure/*`; `presentation` 80 % sentencias/funciones/líneas, 75 % ramas)                                                | `npm run test:coverage` (reporte `text`/`html`/`lcov`)                                                                                                                                                                                         |
| **Caja negra (14 casos)**                                                 | Ver tabla §2                                                                                                                                                                                                                                                                          | Suite de tests de dominio/aplicación, cada caso mapeado a su test                                                                                                                                                                              |
| **ISTQB** (partición de equivalencia, valores límite, tablas de decisión) | Ej.: `money.test.ts` (0, negativos), `interestRate.test.ts` (0 %, 100 %, fuera de rango), `term.test.ts` (plazo 1, plazo máximo), tabla de decisión sistema × estrategia en `extraPaymentSimulator.test.ts`                                                                           | Los propios archivos de test — cada `describe`/`it` documenta la partición o el límite cubierto                                                                                                                                                |

## 2. Los 14 casos de prueba base (caja negra) → test que los verifica

| #   | Caso                                          | Test                                                                                         |
| --- | --------------------------------------------- | -------------------------------------------------------------------------------------------- |
| 1   | Préstamo francés básico                       | `src/domain/loans/frenchAmortization.test.ts`, `src/domain/loans/amortizationEngine.test.ts` |
| 2   | Préstamo alemán básico                        | `src/domain/loans/germanAmortization.test.ts`                                                |
| 3   | Préstamo sin intereses (0 %)                  | `src/domain/loans/frenchAmortization.test.ts` (caso `monthlyRate = 0`)                       |
| 4   | Abono extraordinario único                    | `src/domain/loans/extra-payments/extraPaymentSimulator.test.ts`                              |
| 5   | Múltiples abonos                              | `src/domain/loans/extra-payments/extraPaymentSimulator.test.ts`                              |
| 6   | Abono que reduce plazo                        | `src/domain/loans/extra-payments/reduceTermStrategy.test.ts`                                 |
| 7   | Abono que reduce cuota                        | `src/domain/loans/extra-payments/reducePaymentStrategy.test.ts`                              |
| 8   | Abonos mensuales (recurrentes)                | `src/domain/loans/extra-payments/recurringContribution.test.ts`                              |
| 9   | Préstamo que termina antes del plazo original | `src/domain/loans/extra-payments/extraPaymentSimulator.test.ts` (caso `earlyPayoff`)         |
| 10  | Abono superior al saldo pendiente             | `src/domain/loans/extra-payments/extraPaymentSimulator.test.ts` (capado a saldo)             |
| 11  | Tasas diferentes                              | `src/domain/investments/scenarioComparator.test.ts`                                          |
| 12  | Plazos diferentes                             | `src/domain/investments/scenarioComparator.test.ts`                                          |
| 13  | Comparación francés vs. alemán                | `src/domain/loans/amortizationComparison.test.ts`                                            |
| 14  | Comparación invertir vs. abonar deuda         | `src/domain/investments/debtVsInvestmentComparator.test.ts`                                  |

## 3. Flujos completos → E2E (Fase 13)

| Flujo                                                                                          | Spec                                   |
| ---------------------------------------------------------------------------------------------- | -------------------------------------- |
| Préstamo básico → dashboard → tabla                                                            | `e2e/loan-flow.spec.ts`                |
| Abono extraordinario (único y recurrente) → cuota actual → ahorro → comparación de estrategias | `e2e/extra-payment-flow.spec.ts`       |
| Exportación CSV/XLSX                                                                           | `e2e/export-flow.spec.ts`              |
| Comparador de escenarios                                                                       | `e2e/scenario-comparator-flow.spec.ts` |
| Inversión vs. deuda + disclaimer                                                               | `e2e/debt-vs-investment-flow.spec.ts`  |
| Accesibilidad (axe-core, WCAG 2A/2AA) + teclado                                                | `e2e/accessibility.spec.ts`            |
| Responsive (laptop/tablet)                                                                     | `e2e/responsive.spec.ts`               |
| Humo                                                                                           | `e2e/smoke.spec.ts`                    |

## 4. Estado al cierre (Fase 14)

- 45 archivos de test unitarios/integración (Vitest), 8 specs E2E (Playwright).
- `npm run test`, `npm run lint`, `npm run build`, `npm audit --audit-level=high` y `npm run test:e2e` en verde.
- Los 14 casos de caja negra y los 8 flujos E2E arriba listados cierran el requerimiento de trazabilidad caja negra ↔ test.

## 5. Funcionalidad agregada después del cierre de Fase 14

El proyecto siguió evolucionando después del cierre formal de las 14 fases. Estado actual: **46 archivos de test unitarios/integración, 347 tests** (Vitest), **8 specs E2E** (Playwright, 15 tests). Adiciones y su verificación:

| Adición                                                                                   | Dónde se implementa                                                                                                                | Cómo se verifica                                                                                                           | Referencia                                                                                                             |
| ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| ROI simple de la inversión                                                                | `investmentCalculator.ts` (`InvestmentResult.roi`), `debtVsInvestmentComparator.ts` (`investmentRoi`), `DebtVsInvestmentPanel.tsx` | `investmentCalculator.test.ts` (`describe('roi')`), `debtVsInvestmentComparator.test.ts`, `DebtVsInvestmentPanel.test.tsx` | [ADR 0009](adr/0009-roi-simple-no-anualizado.md)                                                                       |
| Aportes recurrentes en la UI (ya existían en el dominio desde Fase 5, sin UI hasta ahora) | `loanStore.ts` (`recurringContributions`, `addRecurringContributionRow`/etc.), `LoanForm.tsx`                                      | `loanStore.test.ts`, `LoanForm.test.tsx`, `e2e/extra-payment-flow.spec.ts`                                                 | —                                                                                                                      |
| "Cuota actual" en vez de "Variable" con estrategia reducir cuota                          | `Dashboard.tsx` (`currentInstallment`)                                                                                             | `Dashboard.test.tsx` (`'Cuota actual'`)                                                                                    | [ADR 0010](adr/0010-cuota-actual-y-comparacion-de-estrategias.md)                                                      |
| Descripción, conclusión calculada y tooltips formateados en los 6 gráficos                | `chartData.ts` (`formatCurrency`), cada `*Chart.tsx` en `src/presentation/components/charts/`                                      | `chartData.test.ts`, cada `*Chart.test.tsx`                                                                                | —                                                                                                                      |
| Gráfico "Reducir plazo vs. reducir cuota"                                                 | `StrategyComparisonChart.tsx`, `chartData.ts` (`toStrategyComparisonPoints`), `loanStore.ts` (`extraPaymentInputs`)                | `StrategyComparisonChart.test.tsx`, `chartData.test.ts`, `ChartsPanel.test.tsx`, `e2e/extra-payment-flow.spec.ts`          | [ADR 0010](adr/0010-cuota-actual-y-comparacion-de-estrategias.md), [especificación §6.1](especificacion-matematica.md) |
